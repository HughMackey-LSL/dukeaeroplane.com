{
  "name": "dukeaeroplane-eleventy-prototype",
  "private": true,
  "description": "Eleventy build of dukeaeroplane.com with TinaCMS editing.",
  "scripts": {
    "start": "eleventy --serve --port=8082",
    "build:eleventy": "eleventy",
    "dev": "tinacms dev -c \"eleventy --serve --port=8082\"",
    "build": "tinacms build && eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "tinacms": "^2.0.0",
    "@tinacms/cli": "^1.5.0"
  }
}
